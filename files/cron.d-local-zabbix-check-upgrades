# Distributed via ansible - mit.zabbix-agent.check-upgrades

#19078: needrestart auf otrs01 sichten -> Use of runtime loader module Module"
*/5 * * * *     root    /usr/local/sbin/zabbix-check-upgrades 2>&1 | grep -vE "(Use of runtime loader module Module|Unknown option:)"

